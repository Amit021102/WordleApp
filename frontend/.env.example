# Copy to .env and adjust. Vite only exposes variables prefixed with VITE_.
#
# Base URL of the backend API. Must match where uvicorn is listening, and the
# frontend's own origin must appear in the backend's WORDLE_CORS_ORIGINS.
VITE_API_BASE_URL=http://127.0.0.1:8000
